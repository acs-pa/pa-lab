# SPDX-License-Identifier: BSD-3-Clause

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run all solutions
        run: utils/sol_run_all.sh
